# Cross-repository verification — September 21, 2026

The sibling website repository owns the single current [40-item findings register](../../../core-metrics-website/docs/audit/FINDINGS.md), [verification report and delivery evidence](../../../core-metrics-website/docs/audit/VERIFICATION.md), [ordered launch checklist and owner inputs](../../../core-metrics-website/docs/audit/OWNER_ACTIONS.md), [product/data inventory](../../../core-metrics-website/docs/audit/PRODUCT_AND_DATA.md) and [legal/Apple source matrix](../../../core-metrics-website/docs/audit/LEGAL_APPLICABILITY.md). These links assume sibling checkouts. This bridge does not duplicate the findings register.

## Latest verification and delivery

The user explicitly requested fixes, separate commits and pushes. The native implementation through `919b262` and website quiet redesign `ddd62c5`/documentation `7150078` were already on remote main when this final pass began. The new website CI commit `8096adc` adds sequential Firefox and WebKit production journeys to the existing Chromium, publication-state and real-error checks. Exact completed CI/deployment results are maintained in the shared verification report.

Fresh validator `Iiubea` used Xcode 27 RC `27A266a` through a per-command override. Debug/Release builds, **71 Swift Testing declarations /169 execution leaves**, analysis, source/packaged plist and privacy checks, Release fixture exclusion and configuration fingerprints pass. There are zero failed or skipped unit declarations. An initial invocation inherited the machine-selected beta toolchain; only that task-owned run was stopped and superseded by the RC run. It is not the final validation evidence.

The desktop phase still exits 65: XCTest times out enabling automation before any of the 13 app cases starts. Five known AppIntents warning lines, zero other build warnings and zero DisplayManager rectangle diagnostics were recorded. Direct app access works again. Read-only `DevToolsSecurity -status` reports disabled developer authorization, and an unrelated simulator test process was active. Neither observation establishes the timeout's cause. No security/automation settings or unrelated processes were changed. An isolated authorized desktop session is still needed for the full A3 matrix.

A limited manual check of the **installed version 1.0/build 1**, distinct from TestFlight 1.0(2), confirms all four display modes visibly update the Settings preview while retaining full spoken reading context; Metric Help opens and Escape dismisses it; Privacy opens and Return dismisses it. The original Label and Value mode, three selected readings and scroll position were restored. Real clipboard and Launch at Login settings were untouched. This is not current-candidate popover geometry, VoiceOver, sleep/wake or login-cycle acceptance. One computer-use scroll action returned a no-window error; setting the exposed scrollbar succeeded. No application defect was inferred from that tool error.

The website's live HTTPS preview again passes **18/18 focused Chromium checks** (25.3s), and **13/13 unit checks**, 298-string content parity, type generation, TypeScript and lint pass. The already completed redesign matrix passed **74 Chromium /38 WebKit**, three isolated publication states and three genuine server/root recovery scenarios. Firefox's macOS startup problem also occurs with a fresh isolated copy of the pinned browser, before page load. Its symptom matches [Mozilla issue 2060476](https://bugzilla.mozilla.org/show_bug.cgi?id=2060476); Linux CI coverage is separate from a local repair. No profile reset or privacy-permission change was made.

## Candidate and publication boundary

Existing TestFlight **1.0(2)** was built from `96edf35`, before the dead-code cleanup `4906017`. The existing archive/installer signature, sandbox/hardening, manifest and Release exclusion checks were verified in the earlier same-day reconciliation. No new archive, export, upload, installed-app replacement, invitation or submission occurred. The recorded request remains zero testers. A fresh read-only App Store Connect visit now reaches sign-in, so current beta review and public-version fields remain unverified. Historical beta metadata is not evidence of saved public-version metadata.

Website publication remains preview/noindex, with app release preparation. The actual publication gate still fails on seven factual/legal inputs: hosting practices; activity-specific lawful grounds; retention/deletion; recipients/locations/transfers; effective date; final equivalent EN/RO approval; EULA choice. Known company, provider and 30-day support facts must not be requested again. Owner decisions and operational evidence, required real candidate screenshots and current Connect fields remain in the shared checklist.

## History and preserved work

The initial implementation audit comprised native `4906017`/`4500933` and nine website commits after `9214768`. The reconciliation added `90d5e25`/`6fa1efc` and website `ad05414`; later website design, recovery and SEO work is separately recorded. The September 14 publishing review and September 20 TestFlight notes were pre-existing local work. They are now reviewed for the user's explicit commit-all request and retained as dated historical records, with current-status links added. Their historical observations have not been silently presented as new measurements.

A second review covers runtime cancellation/ownership, native status/anchor/Settings paths, privacy/resources/migrations, website routing/publication/recovery and changed documentation. No further confirmed production-code defect justified a speculative edit. No signing identity, entitlement, target, dependency, privacy declaration or operational publication setting was changed. Archives, dSYMs, credentials, caches and unrelated work were retained.
